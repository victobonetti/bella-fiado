export default interface Iproducts {
    _id:string,
    name: string,
    price: number
}

export default interface IAccounts{
    _id:string,

}

export default interface Iuser {
    _id:string,
    username: string,
    password:string,
    // accounts: Array<any>,
    // tokents:Array<any> 
}